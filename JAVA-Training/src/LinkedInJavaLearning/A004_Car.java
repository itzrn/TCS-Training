package LinkedInJavaLearning;

import java.awt.*;
// awt -> abstract window toolkit

public class A004_Car {
    // attributes
    int averageMilesPerGallon;
    String licensePlate;
    Color paintColor; // Java already contains the Color class
    boolean areTailingWorking;

    // above we have noticed that we are not giving value to attributes bcz this is a blueprint.
    // to create car we need something called constructor (here we initialize the attributes)
}
